// Copy this file to `config.js` and fill in your Supabase project values.
// WARNING: `config.js` is gitignored by default. Do NOT commit your secret keys to a public repository.

// Example:
// window.SUPABASE_URL = "https://your-project-ref.supabase.co";
// window.SUPABASE_ANON_KEY = "your-public-anon-key";

window.SUPABASE_URL = "https://your-project-ref.supabase.co";
window.SUPABASE_ANON_KEY = "your-public-anon-key";

// Once you copy -> `config.example.js` to `config.js` and replace the placeholders,
// your app will load the values at runtime and initialize the Supabase client.
